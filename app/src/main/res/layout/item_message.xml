<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Bong bóng chat của AI (Căn trái, màu xám) -->
    <TextView
        android:id="@+id/tvAiMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginEnd="50dp"
        android:background="#E4E6EB"
        android:padding="12dp"
        android:text="Đây là tin nhắn của AI"
        android:textColor="#050505"
        android:textSize="15sp"
        android:visibility="gone" /> <!-- Mặc định ẩn -->

    <!-- Bong bóng chat của User (Căn phải, màu xanh) -->
    <TextView
        android:id="@+id/tvUserMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginStart="50dp"
        android:background="#0084FF"
        android:padding="12dp"
        android:text="Đây là tin nhắn của tôi"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:visibility="gone" /> <!-- Mặc định ẩn -->

</LinearLayout>